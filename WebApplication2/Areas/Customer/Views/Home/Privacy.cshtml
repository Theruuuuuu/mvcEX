@model IEnumerable<WeatherVM>
@{
    ViewBag.Title = "What's up!!";
}
@{
    var name ="臺北市";
}


<div class="text-center">
    <h1 class="display-4">Welcome</h1>    
    <div id="carouselExampleControls" class="carousel slide" data-bs-ride="carousel">
      <div class="carousel-inner">
        <div class="carousel-item active">
          <img src="~/img/123.png" class="d-block w-100" alt="tu" width = "250" height="500">
        </div>
        <div class="carousel-item">
          <img src="~/img/194734621_2828831800764438_1396420422194971323_n.jpg" class="d-block w-100" alt="xi" width = "250" height="500">
        </div>
        <div class="carousel-item">
          <img src="~/img/saul.jpg" class="d-block w-100" alt="saul" width = "250" height="500">
        </div>
      </div>
      <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
      </button>
      <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
      </button>
    </div>
    
</div>

<div class="mb-3 col-6">	
	<select id="sele"  asp-items="ViewBag.SelectList" class="form-select">
		<option disabled selected>--Select CoverType--</option>
	</select>
</div>
<span id="span1"></span>

@*<div class="p-2">
    <ul class="list-group list-group-horizontal-sm">
        @foreach(var location in ViewBag.Loca)
        {
            <a style="text-decoration:none;" asp-controller="Home" asp-action="Privacy" asp-route-location="@location" >
                <li class="list-group-item">@location</li>
            </a>
        }
        <a style="text-decoration:none;" asp-controller="Home" asp-action="Privacy" asp-route-location="" >
            <li class="list-group-item">All</li>
        </a>
    </ul>
</div>*@

@*<div class="container">
    <div class="row g-2">
        @foreach(var item in Model)
        {
            <partial name="Weather" model="item" />
        } 
    </div>    
</div>*@

<div id="partialViewContainer"></div>

<div class="container p-3">
      <iframe src="https://www.youtube.com/embed/j6sqs3jEMe8" title="刃牙武藏道｜劍已是不便之物" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
</div>

<input id="search" />
<div id="searchRes"></div>

@section Scripts{
    <script>
        //$(document).ready(function(){
        //    $("#sele").change(function(){
        //        var a = $("#sele Option:Selected").text();
        //        $("#span1").text(a);
        //    })            
        //})

    $(document).ready(function () {
        // 當 Select 的值改變時，載入對應的部分視圖內容
        $("#sele").change(function () {
            var option = $("#sele Option:Selected").text();
            $.ajax({
                type: 'GET',
                url: "/Customer/Home/Weather?option=" + option, // 請自行替換成你的部分視圖控制器的路徑
                success: function (result) {
                    $('#partialViewContainer').html(result);
                },
                error: function (xhr) {
                    console.log(xhr.responseText);
                }
            });
        });

       $("#search").keyup(function(){
           var res = $("#search").val();
           console.log(res);
            var a = $("#search").text();
            $("#searchRes").text(a);
        });
    });
    </script>
}

  


